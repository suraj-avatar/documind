version: '3.9'

services:
  api:
    build: .
    container_name: documind-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Mount stores as volumes so data persists across container restarts
      - ./vectorstore:/app/vectorstore
      - ./parent_store:/app/parent_store
      - ./bm25_cache.pkl:/app/bm25_cache.pkl
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s
